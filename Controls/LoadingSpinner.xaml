<UserControl x:Class="SpaceTradersWPF.Controls.LoadingSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="{DynamicResource DialogBackgroundSemiTransparentSolidColorBrush}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Image Source="{DynamicResource SpinnerSvgIcon}"
           Width="50"
           Height="50"
           Margin="10">
        <Image.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard Storyboard="{StaticResource BlinkAnimation}" />
            </EventTrigger>
        </Image.Triggers>
    </Image>
</UserControl>
